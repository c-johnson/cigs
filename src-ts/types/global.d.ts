/* TODO: 
  - Go through and add actual types for these `window` properties
*/

declare global {
  interface Window {
    test?: any;
    imageFile?: any;
    imageObj?: any;
    sampler?: any;
  }
}

export {};
